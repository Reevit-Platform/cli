
CLI credentials
  ✔ API key configured (test mode)
  ✔ key authenticates against <API>

Project files
  ✔ nextjs project
  ✖ project manifest is missing — run `reevit init`
  ✖ SDK not installed — run `reevit init` or add @reevit/node
  Environment: .env.local
  • REEVIT_ORG_ID is not set — SDK clients need it alongside the key

Platform sandbox
  • platform bootstrap check skipped until login and init are complete

Running application
  • checkout app is not running — start it with `pnpm dev`, then rerun `reevit doctor --strict` (http://localhost:1/reevit-demo)

Signed webhooks
  ✔ webhook integration not selected

2 problem(s) found — fix the ✖ items above and rerun `reevit doctor`.
