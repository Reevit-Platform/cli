import React from "react";
import ReactDOM from "react-dom/client";
import { ReevitCheckoutButton } from "./components/ReevitCheckoutButton";

ReactDOM.createRoot(document.getElementById("reevit-demo"){{if .TS}}!{{end}}).render(
  <React.StrictMode>
    <main>
      <h1>Reevit test checkout · GHS 50.00</h1>
      <p>No real money will move.</p>
      <ReevitCheckoutButton amount={5000} currency="GHS" />
    </main>
  </React.StrictMode>,
);
