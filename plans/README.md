# Implementation Plans

Generated by the improve skill on 2026-07-07 (audit of `frontend/` + `backend/`, standard depth).
Planned at: root `a450e8a`, frontend `5bd5ced`, backend `61332193`.

Session ran non-interactively, so per the skill's default the top findings by leverage were
planned without a user selection round. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Clear frontend critical/high npm advisories (1 critical, 11 high) | P1 | M | — | TODO |
| 002 | Frontend hardening & DX quick wins (redirect guard, color validation, proxy bodies, vitest excludes, .env.example) | P1 | S | — | TODO |
| 003 | Eliminate per-row customer fetches on the payments table (backend enrich + frontend pass-through) | P2 | M | — | TODO |
| 004 | Scope the features store by org+mode (stale-flag leak on switch) | P2 | M | — | TODO |
| 005 | Backend pre-commit gate + fix stale ROADMAP/CLAUDE.md doc drift | P2 | S | — | TODO |
| 006 | Split backend god-files (handlers_payments 2,458 / auth_repo 2,402 / handlers_auth 1,794 lines) | P3 | L | — | TODO |
| 007 | Merchant-first landing restructure + /developers page (grill session 2026-07-11; phase 0 = live credibility bugs, shippable alone) | P1 | L | content inputs for its §1.7 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies. Soft ordering: run 001 before/after (not during) 002 — both touch `frontend/package.json`-adjacent state and the three-lockfile trio; land one before starting the other.
- 006 must not run concurrently with any other backend-touching work in `adapters/http/` (shared-checkout clobber hazard documented in the repo).

## Direction findings (options, not ranked against the bugs)

Presented for the maintainer to weigh; each would become a design/spike plan if selected:

1. **Finish the A1 billing engine** — ROADMAP.md marks it ~60% built: `plans`/`org_subscriptions`/`org_usage` tables and the Redis metering worker exist; the monthly billing-cycle worker (base + overage invoice generation + charge) and the coupons API are the missing halves. This is the revenue-conversion path. Coarse effort: L (roadmap estimates 3–4 weeks).
2. **One-command E2E payment-lifecycle baseline** — `go test ./adapters/http` requires a live test DB and isn't wired as a single reproducible command/CI job for the full create→confirm→refund→renew cycle. A docker-compose-backed `make test-e2e` + CI job would make every risky plan (incl. 006) safer. Coarse effort: M.

## Findings considered and rejected (do not re-audit)

- **"Open redirect / javascript: URI via payment-link redirect_url" as HIGH** — backend `internal/infra/security/ssrf.go` `ValidateURL` enforces http/https on both create (`paymentlinks/service.go:529`) and update (`:300`) paths; downgraded to frontend defense-in-depth, folded into plan 002.
- **Merge-conflict markers in `payouts/page.tsx` + `content/changelog.ts` breaking tsc** — false; no markers present at `5bd5ced` (subagent read a stale `.claude/worktrees` copy).
- **Committed frontend `.env` secrets** — false; `.gitignore` covers `.env*`, `git ls-files`/history show the files were never tracked. (Local files do hold real keys — normal. The `.env.example` gap is handled in plan 002.)
- **Mode cookie missing HttpOnly** — invalid; the cookie is set client-side via `document.cookie` (`lib/auth/mode.ts`), which cannot set HttpOnly, and the mode value is deliberately client-managed UI state.
- **PASETO key entropy not checked** — already shipped: the #323 boot guard rejects default/short/low-distinct-byte keys in production (backend CLAUDE.md, Jul 6 2026).
- **"Parallelize sequential PSP Confirm calls" (`service.go:1767`)** — mis-attributed evidence; the cited loop performs DB lookups (`GetByScopedProviderRefID`), not provider network calls.
- **"A/B tests not integrated into the router" (ROADMAP C1)** — false; `internal/usecase/payments/payment_router.go:95` consults `resolveABTest` during routing. The stale roadmap sentence itself is fixed by plan 005.
- **Blocklist/scheduledpayment domain packages untested** — the domain packages are type definitions; the logic lives in `internal/usecase/blocklists/` which has `normalize_test.go` + `service_test.go`.
- **M-Pesa/PawaPay webhook IP allowlist fail-open until configured** — documented decision (#324); enforcement flips on when operators provision the CIDR env vars.
- **Payout balance check fail-open** — documented decision (#330); the PSP stays the final authority.
- **Three committed lockfiles** — documented tradeoff; consolidation tracked separately (frontend issue #418).
- **Remove Interswitch/Opay stub providers** — not worth doing; ROADMAP explicitly parks them pending demand, stubs are ~2KB each and inert.
- **Icon library / recharts bundle weight** — MED-confidence smell only; needs a `next build --analyze` measurement before any plan. Left as an investigate-candidate, not planned.
- **Falsy-0 offset call-site bug** — no buggy call site found; the pitfall is already documented in frontend CLAUDE.md.
- **Go 1.27 upgrade** — speculative version claim; go.mod is on a current Go release.
- **Failures page `limit: 100` truncation** — documented decision (frontend CLAUDE.md v1.9.23-era); monitoring idea noted but not planned.
